## 1. 数据层

- [x] 1.1 创建 `src/data/about.ts`，导出 `bioParagraphs`（3 段简介字符串数组）和 `brandTags`（品牌标签字符串数组，含「赋范空间」）

## 2. 组件层

- [x] 2.1 创建 `src/components/About.tsx`，实现左右双栏布局（照片 + 简介）、品牌标签展示、亮/暗主题适配、移动端响应式、照片懒加载与加载失败降级

## 3. 页面集成

- [x] 3.1 在 `src/App.tsx` 中导入 About 组件，插入到 `<Hero />` 与 `<Projects />` 之间
- [x] 3.2 在 `src/components/NavBar.tsx` 的 links 数组中新增「关于我」条目，href 指向 `#about`

## 4. 验证

- [x] 4.1 启动开发服务器，验证 4 个场景：桌面端/移动端布局、亮/暗主题切换、导航栏锚点跳转、照片加载失败降级：桌面端/移动端布局、亮/暗主题切换、导航栏锚点跳转、照片加载失败降级
